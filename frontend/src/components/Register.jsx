import React, { useRef } from "react";

const Register = () => {
  return (
    <div
      className="bg-gray-800 text-white p-6 rounded-lg shadow-lg w-96"
      style={{ position: "absolute", margin: "20rem auto 0 35rem" }}
    >
      Registrations are yet to open. Please check back later. Until then, play
      with test credentials.
    </div>
  );
};

export default Register;
